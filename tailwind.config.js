export default {
    content: [
        "./frontend/**/**/*.{js,jsx,ts,tsx}",
        "./core/server/templates/*.{html,js,jsx,ts,tsx}",
        "./locales/**/*.json"
    ],
    theme: {
        // 在 v4 中，darkMode 已经有所变化
        // 不再需要 darkMode: 'class' 配置
    },
}; 